import type { LucideIcon } from 'lucide-react-native'
import { ActivityIndicator, Pressable, StyleSheet, Text } from 'react-native'
import { colors, radius } from '../styles/theme'
export default function PrimaryButton({ title, onPress, loading=false, disabled=false, icon:Icon, secondary=false }: {title:string;onPress:()=>void;loading?:boolean;disabled?:boolean;icon?:LucideIcon;secondary?:boolean}) { return <Pressable disabled={disabled||loading} onPress={onPress} style={({pressed})=>[styles.button,secondary&&styles.secondary,(disabled||loading)&&styles.disabled,pressed&&styles.pressed]}>{loading?<ActivityIndicator color={secondary?colors.primary:colors.white}/>:<>{Icon&&<Icon color={secondary?colors.primary:colors.white} size={18}/>}<Text style={[styles.text,secondary&&styles.secondaryText]}>{title}</Text></>}</Pressable> }
const styles=StyleSheet.create({button:{minHeight:50,borderRadius:radius.md,backgroundColor:colors.primary,alignItems:'center',justifyContent:'center',flexDirection:'row',gap:8,paddingHorizontal:18},secondary:{backgroundColor:colors.primarySoft},text:{color:colors.white,fontWeight:'900'},secondaryText:{color:colors.primary},disabled:{opacity:.55},pressed:{transform:[{scale:.985}]}})
