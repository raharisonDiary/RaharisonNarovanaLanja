# Aurora Civic V7.0.1

## Correction

- Remplace les proprietes `backgroundColor` et `translucent` du composant `StatusBar` Expo par une configuration compatible : `<StatusBar style="dark" />`.
- Conserve le design Aurora Civic V7.0 et toutes les fonctionnalites existantes.
- Messages PowerShell rendus compatibles avec Windows PowerShell 5.1 pour eviter les caracteres mal affiches.
