# Census Flow V5.2

Correctifs mobiles universels :

- stockage de session avec `expo-secure-store` sur Android/iOS ;
- stockage de session avec `localStorage` sur Web ;
- repli mémoire si le stockage du navigateur est indisponible ;
- résolution TypeScript de `MapScreen` tout en conservant la carte native ;
- carte Web séparée de `react-native-maps` ;
- correction du typage de la route de détail d'un ménage local ;
- conservation des corrections V5.1 sur les thèmes et traductions.

Aucune base de données, variable `.env` ou dépendance npm n'est modifiée.
